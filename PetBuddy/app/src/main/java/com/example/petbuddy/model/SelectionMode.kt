package com.example.petbuddy.model

// selection pet mode
enum class SelectionMode {
    SINGLE,     // เลือกตัวเดียว (Health, Weight, Vaccination)
    MULTIPLE    // เลือกหลายตัว (Feeding, Schedule)
}